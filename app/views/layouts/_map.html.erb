<div style='width: 800px;'>
  <div id="multi_markers" style='width: 800px; height: 400px;'></div>
</div>

<script type="text/javascript">
  $(function() {
      // var lat = <%= @lat %>
      // var lng = <%= @lng %>
      var assignee_coords = <%= @assignee_coords_array.to_json.html_safe %>;
      var todo_coords = <%= @todo_coords_array.to_json.html_safe %>;
      console.log("assignee coords:" + assignee_coords + " todo coords:" + todo_coords);
      handler = Gmaps.build('Google');
      
      handler.buildMap({
          provider: {
            disableDefaultUI: false
            // pass in other Google Maps API options here
          },
          internal: {id: 'multi_markers'}
        }, 

        function(){
          var markers = handler.addMarkers(assignee_coords);
        
          handler.bounds.extendWith(markers);
          handler.fitMapToBounds();
        }
      );
  });
</script>

<script src="https://www.maps.google.com/maps/api/js?v=3.18&sensor=false&client=&key=AIzaSyBMxa7ucqwX08z6wLjiuAj_YdCtTnXjWYY"></script> 
<script src="https://google-maps-utility-library-v3.googlecode.com/svn/tags/markerclustererplus/2.0.14/src/markerclusterer_packed.js"></script>
<script src='https://google-maps-utility-library-v3.googlecode.com/svn/tags/infobox/1.1.9/src/infobox_packed.js' type='text/javascript'></script> <!-- only if you need custom infoboxes -->